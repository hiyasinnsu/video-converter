# 🎬 クライアントサイド動画軽量化・変換ツール

スマートフォンのブラウザ上で完結し、**サーバーへの動画アップロードを一切行わずに** 動画の解像度やビットレートを調整してファイルサイズを劇的に削減できるWebアプリケーションです。

---

## 🌟 特徴
- **動画アップロード不要**: 動画ファイルはお手元のスマートフォン端末内だけで処理されます。初回ページ読み込みには通信が発生します。ページを開いた後の変換は端末内で行います。オフラインでの再読み込みは保証しません。
- **解像度・ビットレートの数値指定**:
  - 幅・高さをピクセル単位で直接入力可能（アスペクト比固定連動）。
  - ビットレート（kbps）のスライダー＆数値直接指定。
  - ワンタップ解像度プリセット（1080p, 720p, 480p, 360p, 50%縮小）。
- **スマホ最適化 (Mobile-First)**: タップしやすい大型ボタン、テンキー入力対応。
- **インストール不要・無料**: GitHub Pagesなどの静的Webサーバーに配置するだけで即座に誰でも利用可能。

---

### 公開URL (GitHub Pages)
👉 **https://hiyasinnsu.github.io/video-converter/**

### リポジトリ
👉 **https://github.com/hiyasinnsu/video-converter**

---

## 🛠 技術構成
- HTML5 Canvas API (フレームスケーリング・縮小描画)
- HTML5 MediaRecorder API (クライアントサイド録画。エンコーダーはブラウザ依存)
- Web Audio API (音声トラックの同期キャプチャ・合成)
- ピュア Vanilla JavaScript & CSS3 (外部ライブラリ依存ゼロ)


## 検証・制約

監査結果、現在の仕様と未検証事項は [project.md](project.md) を参照してください。既存のPlaywright・Chrome・ffprobe・ffmpegが利用可能なら `node audit-test.cjs` で回帰テストを実行できます。スマホ実機・長時間動画は未検証であり、サイズ削減・固定FPS・オフライン再読み込みは保証しません。
