# 動画軽量化・変換Webアプリ プロジェクト管理 (project.md)

## 1. プロジェクト概要
- **目的**: スマホブラウザ上で、動画をサーバーへアップロードせずに（端末ローカル＝クライアントサイドで）動画の解像度・画質（ビットレート等）を変換・軽量化するWebアプリケーションの開発。
- **制約・課題**:
  - スマホ（iOS Safari / Android Chrome）でのメモリ制限（大容量動画でのタブクラッシュ防止）
  - 通信量ゼロ（動画アップロードなし、かつライブラリ読み込み通信量も極小化）
  - 処理速度と端末負荷（バッテリー消費・発熱）のバランス

## 2. 技術調査結果（2026年9月時点のWeb検索による事実確認）
- **調査クエリ**:
  - `ffmpeg wasm mobile safari browser support limitations 2025 2026`
  - `WebCodecs mobile browser support Safari Chrome video encoding mp4-muxer`
  - `ffmpeg wasm single thread mobile browser bundle size 2025 2026`
- **判明した事実**:
  1. **ffmpeg.wasm方式**:
     - メリット: 解像度・CRF・ビットレートなど極めて高度な動画変換が可能。
     - デメリット: 初期ダウンロードサイズが約25MB〜30MB（圧縮後）と大きく、「通信量を食わない」という観点で初回アクセス時に通信が発生する。また、スマホブラウザ（特にiOS Safari）はメモリ制限が厳しく、100〜200MB以上の動画ではブラウザタブがクラッシュ（プロセス終了）するリスクが高い。
  2. **標準Web API（Canvas + Video + MediaRecorder + Web Audio API）方式**:
     - メリット: **外部ライブラリ不要（通信量完全0バイト）**。ブラウザ標準のハードウェアエンコーダーを利用するためメモリクラッシュが極めて起きにくく、スマホでも安定。
     - デメリット: 再生（1倍速または倍速）をベースに録画・縮小するため、動画の長さに応じた処理時間がかかる。
  3. **WebCodecs API + mp4-muxer / webm-muxer 方式**:
     - メリット: ライブラリは数十KBと超軽量。等速再生に縛られずフレーム単位で高速エンコード可能。
     - デメリット: ブラウザ対応状況（Android Chrome、iOS 16.4+以降のSafariなど）に依存。

## 3. 採用方針・ユーザー要望（2026-09-18 確定）
- **UIデザイン**: **案2（ワン画面ダッシュボード型）** を採用。
  - スマホ画面でスクロールしながら1画面で「動画選択・プレビュー・詳細設定・変換実行・結果確認」ができる構成。
  - タップターゲットを十分確保（44px以上）、スマホキーボードに適した数値入力（`inputmode="numeric"`）。
- **調整パラメータの数値化**:
  - **解像度（ピクセル数）**: 幅（Width）と高さ（Height）のピクセル数を数値入力可能にし、元のアスペクト比を維持する連動機能（アスペクト比ロック）を実装。また代表的な解像度プリセット（1080p, 720p, 480p, 360p, 50%など）もボタンでワンタップ指定可能にする。
  - **ビットレート値**: 数値入力（kbps / Mbps）＋スライダーで自由に指定可能にする（推奨値ガイドや元動画の推定ビットレートも表示）。
- **コア技術**:
  - ブラウザ標準機能（Canvas + MediaRecorder + Web Audio API）による完全クライアントサイド処理（通信量0バイト）。

## 4. 詳細タスク一覧
- [x] ユーザーへの技術的実現可能性の回答・方式提示
- [x] UIデザイン案の提示とユーザー確認（案2に決定）
- [x] ユーザー要望を反映した実装計画（implementation_plan.md）の更新とユーザー承認の取得（GitHub連携・Pages公開含む）
- [x] UIの実装（`index.html`）:
  - ファイル選択エリア
  - 元動画情報表示（解像度、ファイルサイズ、再生時間、推定ビットレート）
  - 解像度数値入力（幅・高さpx）＆アスペクト比固定チェックボックス＆プリセットボタン
  - ビットレート数値入力（kbps）＆スライダ
  - プレビュー画面（元動画 / 変換動画）
  - 変換進捗バー（%表示、経過時間）
  - 結果表示（変換後サイズ、削減率%、ダウンロードボタン）
- [x] スタイリングの実装（`style.css`）:
  - スマホ最優先（Mobile-First）レスポンシブデザイン
  - タッチしやすいUIコンポーネント（高さ44px以上、`inputmode="numeric"`）
- [x] 変換ロジックの実装（`app.js`）:
  - 動画読み込み & メタデータ取得
  - Canvasリサイズ & 描画ループ（高画質縮小処理）
  - Web Audioによる音声抽出・合成（AudioContext再利用・disconnect安全処理）
  - MediaRecorderによる指定ビットレートでのエンコード
  - 進捗計算とBlobダウンロードURLの生成
  - リソースクリーンアップ（MediaStreamTrackの確実な停止）
- [x] 動作検証:
  - ローカルサーバー起動（HTTP 200レスポンス確認）
  - テスト動画（1080p, 4秒, 音声付き）の生成・配備
  - JavaScript構文チェック（Syntax OK）
  - 通信量ゼロ（外部リクエストなし、オフライン動作可）の確認
- [x] GitHub Pages公開の準備:
  - Gitリポジトリ初期化（`git init`）& コミット完了（`main` ブランチ）
  - `.nojekyll` の配置
  - README.md に詳細なGitHub Pages公開手順を記載
- [x] 検証結果のウォークスルー作成（walkthrough.md）

## 5. エラー履歴と対策
1. **PowerShellでのコマンドチェーン構文エラー**:
   - 原因: `git add . && git commit` のように `&&` を使用したためPowerShellで構文エラーとなった。
   - 対策: Windows PowerShell では `;` または個別実行を使用すること。
2. **AudioNodeの多重接続対策**:
   - 原因: `createMediaElementSource` は同一要素で1度しか生成できず、再接続時に以前のdestinationへの接続が残る。
   - 対策: `audioSourceNode` をキャッシュし、新ストリーム生成時は `disconnect()` してから再接続する設計とした。
