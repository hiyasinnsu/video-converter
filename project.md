# 動画軽量化・変換Webアプリ プロジェクト記録

最終更新: 2026-09-19（実装監査・デバッグ）

## 目的と成功条件

動画をサーバーへアップロードせず、ブラウザ内で解像度・映像ビットレート・出力再生速度を変更する。既存のワン画面ダッシュボード型UIと外部ライブラリ不要の構成を維持する。

今回の成功条件は、キャンセル・失敗・再試行で結果や録画資源が混在しないこと、出力形式と拡張子が一致すること、実装と説明が一致すること、実動画で回帰確認できること。

## 現行設計

- HTML/CSS/JavaScriptのみ。Canvas + MediaRecorder + Web Audio APIを利用。
- 動画のアップロードなし。初回ページ読み込みには通信が必要。ページを開いた後の変換は端末内で行う。Service Workerはなく、オフラインでの再読み込みは保証しない。
- 元動画の縦横比に連動する数値入力、1080p/720p/480p/360p/50%プリセット。出力各辺2〜3840px、偶数に正規化。極端な縦横比などでプリセットが範囲外になった場合は開始時に拒否する。
- 映像ビットレートの入力範囲100〜20000kbps。速度1倍未満では入力値×速度に補正（下限150kbps）。音声・コンテナの容量は別途加わり、設定値どおりのサイズは保証しない。
- 出力速度0.1/0.25/0.5/0.75/1/1.25/1.5/2倍。変換は実時間録画であり、所要時間はおおむね元の長さ÷速度。0.25倍以下の音声を無効にするのは、ブラウザ差を避けるためのアプリ方針。
- requestVideoFrameCallbackで提示されたフレームを描画し、requestFrameを1回要求。未対応ならrequestAnimationFrame、手動キャプチャ未対応ならcaptureStream(30)。厳密な固定FPS・補間・フレーム欠落ゼロは保証しない。
- 録画中は元動画を進捗カードへ移して表示し、非表示動画へのコールバック抑制を避ける。操作は無効にし、終了後に元の位置へ戻す。
- 録画セッションごとにチャンクとキャンセル状態を保持。正常終了時のみ結果を生成し、古いイベントは無視する。エラーは通知して設定画面へ戻す。
- 音声保持を指定して取得に失敗した場合は、無音で成功扱いせず中断する。AudioContextとMediaElementSourceは再利用し、各回の接続・トラックは解放する。
- 対応形式を選択して録画し、実際のMediaRecorder MIMEタイプ（またはチャンクのタイプ）でBlobと拡張子を決定。
- ファイル名は設定・結果欄で同期し、禁止文字を除去。空名は既定名へ補正。同じ出力拡張子の重複を避ける。

## 2026-09-19 監査で確認・修正した事項

| 重要度 | 問題と影響 | 修正 |
|---|---|---|
| 高 | キャンセルでもonstopが完了処理を呼び、途中の動画を成功表示。共有チャンクが再試行と競合 | セッションごとの状態とチャンク、キャンセル時の破棄、古いイベントの無視 |
| 高 | Canvas、Audio、Recorder開始失敗で処理中画面やトラックが残る | 開始から再生まで例外処理。共通クリーンアップ。音声取得失敗を通知 |
| 高 | プレビュー再生中はonplayが再発火せず録画開始できない。先頭へのシーク完了を待たず録画 | プレビューを一時停止、先頭シークとデータを待機、明示的に録画開始 |
| 中 | コーデック指定失敗時の代替形式を元のMIMEで保存する可能性 | 実際のMIMEを使用 |
| 中 | requestFrameの連続呼び出しを等間隔の複数フレーム生成と誤認 | 無効な複製ループを削除。提示フレームごとに1回要求 |
| 中 | captureStream(0)成功・requestFrame未対応の場合は更新が止まる | メソッドの有無でも自動キャプチャへ切り替え |
| 中 | 終了直前50msで録画を切り上げる | endedイベントで終了 |
| 中 | 巨大値・負値などをそのままCanvasやRecorderへ渡す | 開始前に有限値・範囲検証。スライダーと数値欄の範囲も統一 |
| 中 | 不正動画の読み込み失敗が無通知 | 読み込みエラーと不正メタデータを通知し選択画面へ戻す |
| 低 | リセットで古いURL・再生速度・イベントが残る | URL・状態・イベントを解放し速度表示を同期 |
| 低 | 容量増加でも軽量化成功・高画質と表示 | 容量増加を表示し、画質を断定しない |
| 低 | 禁止文字のみ／拡張子のみのファイル名、ビットレートラベル参照先、ズーム禁止、小さい操作部 | 名前の補正、ラベル修正、ズーム許可、対象ボタン・数値欄を44px以上へ |

旧実装をコメントアウトして残す運用は廃止し、Git履歴で管理する。

## 検証と再実行

`audit-test.cjs` は既存のPlaywright・Chrome・ffprobe・ffmpegを使う回帰テスト。アプリ自体には依存を追加しない。ローカルループバックの一時HTTPサーバーを起動し、結果動画はOSの一時フォルダへ保存する。

```powershell
# Playwrightがモジュール検索パスにある環境で実行
node --check app.js
node audit-test.cjs
git diff --check
```

付属のtest_sample.mp4は1920×1080、30fps、4秒、AAC音声付き（ffprobe確認）。

- [x] Chrome実変換: 通常、0.25倍、2倍、キャンセル後の再試行
- [x] 出力640×360、パケット時刻による長さ確認、音声有無と音声信号の確認
- [x] 不正入力、不正動画、Canvas取得失敗、MIME代替形式、requestFrame未対応の検証
- [x] 追加検証: 0.75倍、準備中キャンセル後の即再試行、再生拒否、録画開始失敗、拡張子のみの名前
- [ ] iOS Safari / Android Chrome実機、長時間・大容量・可変フレームレート・縦動画

最終実行は全項目PASS。8回の実変換を検査し、ページ内JavaScriptの未処理例外は0件。`node --check app.js`、`node --check audit-test.cjs`、`git diff --check`も合格。

| 出力速度 | 期待長さ | 最終映像パケットの時刻（実測） | 音声 |
|---|---:|---:|---|
| 1倍 | 4秒 | 4.062秒 | あり・信号確認済み |
| 0.25倍 | 16秒 | 15.881秒 | アプリ方針で除外 |
| 0.75倍 | 約5.333秒 | 5.338秒 | あり・信号確認済み |
| 2倍 | 2秒 | 2.036秒 | OFF指定で除外 |

録画方式のため開始・終了時刻に小さな差が生じる。テストは許容差0.65秒で確認。固定FPSや厳密なフレーム／音声同期の検証ではない。

このPCでは既存のPlaywrightを利用するため、実行前に次を指定した。

```powershell
$env:NODE_PATH = "$env:USERPROFILE\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules"
```

## 制約と今後の作業

- 出力チャンクをメモリに保持してBlobを作るため、大容量動画ではメモリ不足のリスクがある。「クラッシュしない」とは保証しない。
- ハードウェアエンコードの採用、対応コーデック、音声速度補正、実際のビットレートはブラウザ・端末に依存。
- バックグラウンド化・画面ロック・端末負荷によって描画や音声が停止／遅延する可能性がある。変換中はページを前面で開いておく。
- 固定FPSや厳密な音声映像同期が必要なら、別途タイムスタンプを扱うエンコード設計が必要。今回は既存の録画方式を維持。
- スマホ実機での出力再生・保存、長時間動画でのメモリ使用と音ずれを次に確認する。
- 監査後、ユーザーから指定リポジトリへのコミット・pushを依頼された。対象はorigin/main。既存GitHub Actionsはmainへのpushで公開する設定。
- 過去の記録にあったimplementation_plan.md、walkthrough.mdはこのフォルダには存在しない。過去の承認・公開完了や実機検証の証拠として扱わない。

## API仕様の根拠

- [W3C Media Capture from DOM Elements](https://www.w3.org/TR/mediacapture-fromelement/): requestFrameはキャプチャ要求フラグを設定する。連続呼び出しで指定個数の等間隔フレームを生成するAPIではない。
- [W3C MediaStream Recording](https://www.w3.org/TR/mediastream-recording/): stop後のdataavailable/stopイベントと実際のMIMEを考慮する。
